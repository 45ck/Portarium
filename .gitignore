# Dependencies
node_modules/

# Build output
dist/

# Coverage
coverage/

# TypeScript incremental
.tsbuildinfo/

# Environment
.env
.env.*
!.env.local.example

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# QA artifacts (large; keep reports but not raw artifacts)
qa-artifacts/

# Stryker temp
.stryker-tmp/

# Mutation reports (generated; keep folder structure only)
reports/mutation/*
!reports/mutation/.gitkeep

# Beads temp (local install scratch)
_tmp_bd/
package/

# Domain Atlas upstream clones (local-only; do not commit upstream code)
domain-atlas/upstreams/*
!domain-atlas/upstreams/.gitkeep

# Local tool outputs (generated during ad-hoc runs)
knip_debug.txt
lint_out.txt
vitest_*.txt
nul

# Git worktrees (managed by bd issue start/finish)
.trees/
.claude/worktrees/

# Terraform — do not commit real backend configs or local state files
# (only *.example counterparts are checked in)
infra/terraform/**/.terraform/
infra/terraform/**/terraform.tfstate
infra/terraform/**/terraform.tfstate.backup
infra/terraform/**/*.tfplan
infra/terraform/**/backend.tf
infra/terraform/**/*.s3.tfbackend
infra/terraform/**/*.azurerm.tfbackend
infra/terraform/**/*.gcs.tfbackend
infra/terraform/**/.terraform.lock.hcl

# Beads SQLite database (generated; .beads/issues.jsonl is the git-portable format)
.beads/beads.db
.beads/beads.db-shm
.beads/beads.db-wal
.beads/bd.sock
.beads/bd.sock.lock
.beads/deletions.jsonl
