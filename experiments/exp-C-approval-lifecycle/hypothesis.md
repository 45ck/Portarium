# Hypothesis

A governed `write:file` action proposed through the native OpenClaw plugin path
is blocked for operator approval, visible in Cockpit-style approval queries,
unblocked only after an operator approval, and then visible as a succeeded Run
with linked Plan, Approval, Action, and System Evidence entries.
